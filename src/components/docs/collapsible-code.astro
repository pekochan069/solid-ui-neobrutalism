---
import CollapsibleCodeBlock from "./collapsible-code-content";

type Props = {
  class?: string;
};

const { class: className } = Astro.props;
---

<CollapsibleCodeBlock client:load class={className}>
  <slot />
</CollapsibleCodeBlock>
